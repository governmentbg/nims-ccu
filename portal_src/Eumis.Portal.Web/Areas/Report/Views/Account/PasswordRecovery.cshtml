@using Eumis.Portal.Web.Helpers
@model Eumis.Portal.Web.Models.Account.ActivateVM

@Html.Breadcrumb(new List<BreadcrumbItem>()
{
    new BreadcrumbItem(Url.Action(MVC.Report.Home.ActionNames.Index, MVC.Report.Home.Name, new { area = MVC.Report.Name }), Global.HomeButton)
}
, Global.BreadcrumbsActivate)

<div class="container warnings">
    <img class="icon" src="@Url.Content(Links.Content.img.icons.warning_gif)" alt="" />
    <p><strong>@RecoverPassword.InfoP1</strong></p>
    <p>&nbsp;</p>
    <p>@RecoverPassword.InfoP2</p>
    <p>&nbsp;</p>

    <form method="post">
        <section class="clearfix">
            @Html.HiddenFor(m => m.ActivationCode)
            <div class="form-group col-xs-12" style="text-align: left;">
                @Html.ValidationSummary()
            </div>
            <div class="form-group col-lg-4 col-sm-6" style="text-align: left;">
                <label>@RecoverPassword.Password</label>
                @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
            </div>
            <div class="form-group col-lg-4 col-sm-6" style="text-align: left;">
                <label>@RecoverPassword.ConfirmPassword</label>
                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
            </div>
            <div class="col-lg-4 col-sm-12">
                <input type="submit" class="submit m-right-0 wait" value="@Global.ButtonContinueText">
            </div>
        </section>
    </form>
</div>