@model List<R_10018.AttachedDocument>

@if (Model != null)
{
    <div class="table-wrapper">
        <img class="horizontal-scroll" src="@Url.Content(Links.Content.img.icons.blue_arrow_right_gif)" alt="" />
        <table class="chart-info dynamic-rows file-upload" width="100%">
            <thead>
                <tr>
                    <th style="width:17%;">@AttachedDocument.DocumentType</th>
                    <th style="width:9%;">@AttachedDocument.VersionNumber</th>
                    <th style="width:14%;">@AttachedDocument.ActivationDate</th>
                    <th style="width:30%;">@AttachedDocument.Description</th>
                    <th style="width:30%;">@AttachedDocument.FileName</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Count > 0)
                {
                    for (int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(m => m[i].Type)
                            </td>
                            <td>
                                @Html.DisplayFor(m => m[i].RegNumber)
                            </td>
                            <td>
                                @Html.DisplayFor(m => m[i].ActivationDate)
                            </td>
                            <td>
                                @Html.DisplayFor(m => m[i].Description)
                            </td>
                            <td>
                                <div class="files">
                                    @if (Model[i].AttachedDocumentContent != null && !String.IsNullOrWhiteSpace(Model[i].AttachedDocumentContent.FileName) && !String.IsNullOrWhiteSpace(Model[i].AttachedDocumentContent.BlobContentId))
                                    {
                                        <p>@Model[i].AttachedDocumentContent.FileName</p>
                                        <a target="_blank" href="/api/@(this.ViewContext.RouteData.Values[Constants.SessionKey].ToString())/file/download/@(Model[i].AttachedDocumentContent.BlobContentId)" class="download-file file-btn">@Global.ButtonDownloadText</a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
}
