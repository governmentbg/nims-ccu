@model R_10040.SignedContracts

@if (Model != null)
{
    <div class="table-wrapper">
        <img class="horizontal-scroll" src="@Url.Content(Links.Content.img.icons.blue_arrow_right_gif)" alt="" />
        <table class="chart-info dynamic-rows file-upload" width="100%">
            <thead>
                <tr>
                    <th style="width:60%;">@AttachedDocument.Description</th>
                    <th style="width:40%;">@AttachedDocument.FileName</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.SignedContractCollection != null && Model.SignedContractCollection.Count > 0)
                {
                    for (int i = 0; i < Model.SignedContractCollection.Count; i++)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(m => m.SignedContractCollection[i].Description)
                            </td>
                            <td>
                                <div class="files">
                                    @if (Model.SignedContractCollection[i].AttachedDocumentContent != null && !String.IsNullOrWhiteSpace(Model.SignedContractCollection[i].AttachedDocumentContent.FileName) && !String.IsNullOrWhiteSpace(Model.SignedContractCollection[i].AttachedDocumentContent.BlobContentId))
                                    {
                                        <p>@Model.SignedContractCollection[i].AttachedDocumentContent.FileName</p>
                                        <a target="_blank" href="/api/@(this.ViewContext.RouteData.Values[Constants.SessionKey].ToString())/file/download/@(Model.SignedContractCollection[i].AttachedDocumentContent.BlobContentId)" class="download-file file-btn">@Global.ButtonDownloadText</a>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
}
