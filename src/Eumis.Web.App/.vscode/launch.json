{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Lunch chrome",
            "url":"http://localhost:65509",
            "webRoot": "${workspaceFolder}"
        }
    ]
}